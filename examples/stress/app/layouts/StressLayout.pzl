<puzzle-view class="min-h-screen bg-ink text-fg">
  <header class="border-b border-hairline">
    <div class="max-w-6xl mx-auto px-6 py-5 flex items-baseline justify-between gap-6">
      <div>
        <h1 class="text-lg font-semibold tracking-tight text-fg">Puzzle Stress Lab</h1>
        <p class="mt-0.5 text-xs text-muted">
          Deterministic performance scenarios · drive from the page or from
          <code class="font-mono text-accent">window.__STRESS__</code>
        </p>
      </div>
      <div class="font-mono text-[10px] uppercase tracking-[0.28em] text-faint shrink-0">
        seed { seedHex }
      </div>
    </div>
  </header>

  <main class="py-6">
    <Slot/>
  </main>
</puzzle-view>

<script>
import { PuzzleView } from '@magic-spells/puzzle';
import { STRESS_SEED } from '../scenario-utils.js';

export default class StressLayout extends PuzzleView {
  data() {
    return { seedHex: '0x' + STRESS_SEED.toString(16) };
  }
}
</script>
