// Bare-bones config — no Tailwind, no styles pipeline. This app exists to
// measure the Puzzle runtime's baseline size, so nothing but the framework
// itself ends up in the bundle.
export default {};
