<puzzle-view>
  <article class="card {#if active}active{:else}{ tone }{/if}">
    <h2>{ title }</h2>
    <input value={ draft } @input={ update(event) } />
  </article>
</puzzle-view>

<puzzle-skeleton>
  <article>{ loadingLabel }</article>
</puzzle-skeleton>

<script>
import { PuzzleView } from '@magic-spells/puzzle';

export default class PlainCard extends PuzzleView {
  events = {
    update: (event) => event.currentTarget.value,
  };
}
</script>
